<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fajar.learnyourself">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Menu" />
        <activity android:name=".JavaActivity" />
        <activity android:name=".PHPActivity" />
        <activity android:name=".PythonActivity" />
        <activity android:name=".SQLActivity" />
        <activity android:name=".JavaQuiz" />
        <activity android:name=".PHPQuiz" />
        <activity android:name=".PythonQuiz" />
        <activity android:name=".SQLQuiz" />
        <activity android:name=".Welcome" />
        <activity android:name=".MainActivity" />
        <activity android:name=".Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>